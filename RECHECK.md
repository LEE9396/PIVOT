# 재실험 전 점검 — 팀원 PC에서 할 일

`session_20260904_1736` 이 밀도 반폭 **2,743,357 %** (목표 5 %) 로 끝났다.
부위 3 개 중 2 개가 하한 50 kg/m³ 에 정확히 붙었고 잔차 팽창이 5527~6507 이었다.

원인을 분석해 보니 **네 가지가 전부 한 곳을 가리킨다 — F/T 센서 좌표계다.**
카메라와 로봇은 잘 보정돼 있는데, 정작 무게를 읽는 장치만 보정된 적이 없다.

이 문서는 **로봇을 움직이기 전에 30 분 안에** 그 넷의 크기를 숫자로 확인하고,
실험을 시작해도 되는지 판단하는 절차다.

> **중요** — 지금 그대로 재실험하면 같은 실패가 재현된다. 아래 검사를 먼저
> 돌리고, 나온 숫자에 따라 스위치를 켜야 한다. 일부러 기본값을 안 바꿔
> 두었다. 실측 없이 뒤집으면 지금보다 나빠지기 때문이다.

---

## 1. 환경 갱신

새 의존성은 없다. `bootstrap.sh` 를 다시 돌릴 필요 없다.
보정값(`calibration/*.json`)과 설정(`setup/experiment.conf`)은 gitignore 에
있으므로 **지워지지 않는다.**

```bash
cd ~/Desktop/PIVOT

# 지금 작업 중인 것이 있으면 먼저 저장한다
git status --short
git stash            # 필요할 때만

git remote add lee https://github.com/LEE9396/PIVOT.git 2>/dev/null || true
git fetch lee

# lee/master 가 이제 최신이다 (포크의 실험 환경 + 이번 수정이 모두 들어 있다)
git checkout -B recheck lee/master

# 잘 갱신됐는지
git log --oneline -6
ls my_work/tare_check.py tools/check_grasp_frames.py tools/check_tare_poses.py
```

이 커밋들이 보여야 한다.

```
목표 반폭이 이 물체·이 센서로 가능한지 재기 전에 계산해서 찍는다
재실험 전 점검 절차를 팀원 PC 용으로 적는다
목적지와 경로를 같은 세계에서 검사하고, 검사 간격을 안전 여유에서 유도한다
영점 조정을 "쟀다" 에서 "맞다" 로 바꾼다
실측 파지를 밀도 계산까지 잇는 길을 놓고, 토크 기준점 어긋남을 드러낸다
측정 오차의 원인을 찾을 수 있게 고치고 안전 여유를 20 mm 로 올린다
```

### 설정 파일

`setup/experiment.conf` 는 PC 마다 다르므로 저장소에 없다. 직접 고친다.

```diff
  TARE_MAX_AGE_S=0        # 시간으로는 안 막는다 (아래 설명)

- ANGLE_MARGIN_DEG=0.0
+ ANGLE_MARGIN_DEG=5.0    # 사람이 각도를 돌리는 범위까지 충돌 검사
```

**영점의 유효기간을 왜 시간으로 안 재나.** 영점을 못 쓰게 만드는 것은 시간이
아니라 **그리퍼나 F/T 센서를 다시 다는 일**이다. 다시 달면 그리퍼가 만드는
렌치가 통째로 달라지므로 그 즉시 다시 재야 하고, 안 건드렸으면 며칠이 지나도
그대로 쓸 수 있다. 온도 드리프트가 있긴 하지만 그건 `--verify` 로 10 초 만에
확인하는 편이 낫다.

그래서 `TARE_MAX_AGE_S=0` (시간 제한 없음) 으로 두고, 대신 세션을 켤 때
**언제 잰 값인지**를 찍는다. 그리퍼나 센서를 다시 달았으면 그때 다시 재면 된다.

```bash
setup/wrist_tare.sh --run          # 다시 달았을 때
setup/wrist_tare.sh --verify       # 그대로 써도 되나 10 초 확인
```

---

## 2. 손으로 할 일 (10 분) — 이게 검사보다 먼저다

**AFT200 케이블과 그리퍼 배선을 팔에 고정한다.**

- 묶는 위치는 **센서보다 안쪽(로봇 쪽)** 이다. 센서 바깥에서 묶으면 케이블
  장력이 그대로 측정값에 들어간다.
- 센서 바깥 구간에는 **여유(서비스 루프)** 를 준다. 손목이 어떤 자세여도
  케이블이 팽팽해지지 않아야 한다.

왜 이게 먼저인가 — 9/1 자 영점 데이터를 물리 모형에 맞춰 보면 **14.7 N** 이
설명되지 않고 남는다. 같은 자세를 두 번 재면 0.034 N 밖에 차이가 안 나는데도
그렇다. **재려는 램프 전체가 5.6 N 이다.** 영점이 물체보다 2.6 배 무겁게
흔들리는 상태에서는 무엇을 고쳐도 소용없다.

케이블을 안 고치고 영점만 다시 재면 같은 값이 나온다.

---

## 3. 검사 (20 분)

```bash
cd ~/Desktop/PIVOT
R=robot_learning/scripts/run_drake_env.sh
```

### 3-1. 영점 조정을 8 방향으로 다시 잰다

```bash
setup/wrist_tare.sh --plan     # 먼저 경로만. 8 방향이 다 도달되나
setup/wrist_tare.sh --run      # 재고 -> 검산 -> 합격해야 저장
```

예전에는 3 방향이었다. 미지수가 축 3 + 바이어스 3 + 무게 1 = 7 개인데
방향이 3 개면 식이 9 개뿐이라 거의 맞아떨어진다. 그러면 **잔차가 0 에 가깝게
나와서 "값이 맞다" 와 "검산할 여유가 없다" 를 구별할 수 없다.**

손목 한계(120°) 밖인 방향은 경고만 찍고 건너뛴다. 필수 3 방향이 안 되면 멈춘다.
불합격이면 `.failed.json` 으로 남기고 저장하지 않는다.

`--plan` 은 팔을 **어느 자리에 세워야** 손목만으로 세 방향을 다 만들 수 있는지
스스로 찾는다. 로봇이 서 있던 자리로 되면 팔은 아예 안 움직이고, 안 되면 되는
자리까지 가는 경로를 (충돌 검사를 거쳐) 한 번만 계획한 뒤 그 다음부터는 손목만
돌린다. 출력의 `손목 여유` 줄이 J4-J6 가 지금 자리에서 좌우로 몇 도씩 남았는지
보여 준다.

후보는 하나가 아니다. 필수 방향마다 팔 전체로 푼 자세를 여러 개 받아서,
**손목으로 세 방향이 다 되는지**와 **거기까지 갈 충돌 없는 경로가 있는지**를
둘 다 본다. 하나라도 걸리면 다음 후보로 넘어간다. 팔을 적게 움직이는 후보부터
본다.

그래도 배치를 못 찾으면 후보마다 왜 안 됐는지 찍는다. 거기 나온 대로

    창을 넓히면 된다   ->  setup/wrist_tare.sh 에 --max-wrist-deg 180 을 넘긴다
                           (케이블이 그만큼 꼬여도 되는지 눈으로 먼저 확인)
    충돌이 문제다      ->  작업영역을 치우고 다시
    경로를 못 찾았다   ->  --max-iters 40000
    어느 쪽도 아니다   ->  팔을 직접교시로 다른 자세로 옮기고 --plan 다시

로 대응한다. `--plan` 은 로봇에 명령을 보내지 않으므로 몇 번을 돌려도 안전하다.

손에 무엇을 들고 재려면 **반드시 `--payload-mm X Y Z` 로 그 크기를 알려야
한다.** 충돌 검사는 여기서 만든 형상만 보므로, 안 주면 손에 든 물건이
검사에 아예 안 보인다. 손목이 크게 도는 동안 팔이나 책상에 닿아도 프로그램은
여유가 넉넉하다고 말한다. 방향이 헷갈리면 가장 긴 치수로 정육면체를 주면
된다 — 검사가 빡빡해질 뿐 놓치지는 않는다.

### 3-2. 영점이 물리적으로 말이 되는지

```bash
$R python my_work/tare_check.py <TARE_FILE 경로>
```

### 3-3. 토크 기준점과 파지 좌표계

```bash
$R python tools/check_grasp_frames.py --object desklamp \
     --grasp my_work/sessions/<세션>/grasp.json
```

`grasp.json` 은 파지 완료 시점에 만들어진다. 없으면 이번 세션에서 한 번
잡은 뒤에 돌린다.

### 3-4. "같은 자세면 상쇄된다" 전제가 성립하는지

```bash
$R python tools/check_tare_poses.py --tare <TARE_FILE> \
     --object desklamp --theta-deg 24.4 71.4
```

---

## 4. 결과로 판단하기

### 검사 3-2 — 영점

| 항목 | 합격 | 불합격이면 |
|---|---|---|
| 힘 맞춤 잔차 | ≤ 0.5 N | **실험 금지.** 케이블 고정을 다시 한다 |
| 토크 맞춤 잔차 | ≤ 0.02 N·m | 〃 |
| 공구 질량 | 0.9 ~ 1.2 kg | 힘의 크기나 단위가 틀렸다 |
| 토크 지렛대 | 20 ~ 100 mm | 토크가 파지점이 아닌 원점 기준이다 |
| 힘 부호 | 모형과 일치 | ↓ 아래 5-1 |
| 센서 축 정렬 | ≤ 5° | ↓ 아래 5-2 |

**잔차가 0.5 N 을 넘으면 나머지 숫자를 믿을 수 없다.** 케이블부터 고치고
다시 잰다. 여기를 통과하기 전에는 실험을 시작하지 않는다.

### 검사 3-3 — 토크 기준점과 파지

| 나오는 값 | 뜻 |
|---|---|
| 두 기준점 사이 거리 | 이 컴퓨터에서 계산했을 때 **187.7 mm**. 토크 오차 1.05 N·m = 센서 잡음의 350 배. 파지 오프셋 미지수의 허용 범위가 ±50 mm 라 흡수되지 않고 경계에 붙는다 |
| 파지 회전 차이 | **137.9°** 였다. 사람이 만들 수 있는 오차가 아니다 → 좌표계 규약 불일치 |

### 검사 3-4 — 상쇄 전제

| 나오는 값 | 뜻 |
|---|---|
| 관절 차이 최대 | 이 컴퓨터에서 **174°**. 영점 자세와 측정 자세가 전혀 다르다 → 케이블 장력이 상쇄되지 않는다 |

케이블을 고정하고 나면 이 값이 커도 영향이 줄어든다. 원인을 없애는 것이
자세를 맞추는 것보다 쉽다.

---

## 5. 결과에 따라 바꿀 것

### 5-1. 힘 부호가 어긋났다면 — 한 줄

```python
# my_work/density_id_drake.py
FORCE_SIGN = +1.0        # -> -1.0
```

**여기만 바꾼다.** 읽는 쪽에서 또 뒤집으면 원위치가 된다.
`measure()` 와 `regressor()` 가 같은 상수를 쓰므로 **시뮬레이션 결과는 이
값과 무관하게 같다** (확인했다). 바꾼 뒤 `tare_check` 를 다시 돌리면
「힘 부호」가 통과로 바뀌어야 한다.

### 5-2. 센서 축이 5° 넘게 돌아가 있다면 — 코드 수정 (한 줄 아님)

영점 파일의 `measured.sensor_axis_R` 에 3×3 회전이 저장된다. 그 값으로
`robot_scene.build_scene` 의 AFT 장착 변환을 대체해야 한다.

```python
# 지금 — 손으로 넣은 값
RotationMatrix.MakeXRotation(np.pi/2) @ RotationMatrix.MakeZRotation(GRIPPER_MOUNT_YAW_RAD)
```

**이건 결과를 보고 같이 하자.** 회전이 몇 도로 나오는지에 따라 접근이 달라진다.

### 5-3. 파지 회전 차이가 한 자릿수로 내려왔다면

```bash
$R python my_work/dual_view.py ... --grasp-frame measured
```

카메라가 실측한 6D 파지가 밀도 계산에 들어가고, 토크 기준점도 AFT 기준으로
맞춰진다. **회전 차이가 수십 도인 상태에서 켜면 지금보다 나빠진다** — 모멘트팔이
부위당 188~433 mm 씩 달라진다 (측정해서 확인함).

---

## 6. 한 라운드만 시험

위를 통과하면 **한 라운드만** 돌린다. 볼 것은 하나다.

```
잔차 팽창 ≤ 5      성공. 부호와 영점이 전부였다
          5 ~ 70   부분 성공. 남은 계통 오차가 있지만 밀도는 쓸 만하다
          > 100    아직 남았다. 이제 원시 wrench 가 저장되니 즉시 특정 가능
```

그리고 **밀도가 하한 50 kg/m³ 에 안 붙으면** 부호가 고쳐진 것이다.

라운드마다 `exploration_round_N.json` 에 이것들이 남는다.

```
wrench          영점 적용 후
wrench_raw      영점 적용 **전**      <- 이게 있어야 센서/영점을 가른다
tare_applied    실제로 뺀 값
robot_joint_deg 그때 로봇이 어디 있었나
residual_inflation, grasp_offset_mm
```

---

## 7. 이번에 고쳐도 남는 것 — 미리 알아두기

위를 다 고쳐도 **목표 5 % 에는 도달하지 못한다.**

```
                   무게      힘 신호   센서 잡음 대비
시뮬레이션 3link   1.904 kg   18.7 N       47 배
이번 램프         0.571 kg    5.60 N       14 배
```

부위 셋 중 둘이 100 g 미만이다 (연결부 85 g, 머리 87 g). 100 g 은 약 1 N 인데
AFT200 의 실사양 잡음이 0.4 N 이다. **잡음의 2.5 배짜리 신호를 5 % 로 나누는
것은 원리적으로 불가능하다.**

**탐색기가 시작할 때 이걸 직접 찍어 준다.** 잡음 모형 두 개로 나란히 계산한다.

```
  목표 5.0 % 가 이 물체·이 센서로 가능한가  (최선의 경우, 잔차 팽창 1.0 가정)
    설정값 0.10 N / 0.003 N·m  ->  6 라운드에서 도달
    자료값 0.40 N / 0.025 N·m  ->  도달 불가 (바닥 30 %)
```

**어느 잡음 모형을 쓰느냐에 따라 답이 뒤집힌다.** 지금 코드에 박힌 값
(0.10 N)은 1000 개 평균을 독립 잡음으로 본 것이라 낙관적이다 — 그래서 지난
세션이 5 % 를 목표로 잡고도 아무도 이상하다고 못 느꼈다. AFT200 자료값으로
계산하면 **30 % 가 바닥이다.**

**할 일**: 고정 자세에서 10 회 반복 측정해 실제 표준편차를 재고,
`density_id_drake.SIGMA_F / SIGMA_T` 를 그 값으로 바꾼다. 그러면 위 예보가
정직해지고, 목표를 근거 있게 정할 수 있다. 지금 자료값 기준으로는 목표를
**30 % 대**로 잡는 것이 맞다.

선택지는 셋이다.

1. 더 무거운 물체로 바꾼다 (노트북, 큰 스탠드) — 가장 현실적
2. 더 정밀한 소형 F/T 센서로 바꾼다
3. 목표를 20~30 % 로 완화하고 논문을 그렇게 쓴다

**이번 재실험의 목표는 5 % 달성이 아니라 "잔차 팽창을 한 자릿수로 내리는 것"
으로 잡는 편이 낫다.** 그게 되면 측정 사슬이 건강해진 것이고, 정밀도는 물체와
센서를 바꿔서 올리면 된다.

---

## 부록 — 이 컴퓨터에서 잰 참고값

팀원 PC 에서 나오는 값과 비교하라. 자산 부피가 2.5 배 차이 나므로 절대값은
근사치이고, **어긋남의 크기와 방향**이 비교할 부분이다.

| | 값 |
|---|---|
| 영점 힘 잔차 (9/1 파일) | 14.658 N (허용 0.5) |
| 영점 토크 잔차 | 0.654 N·m (허용 0.02) |
| 되찾은 공구 질량 | 1.003 kg (코드 상수는 0.650) |
| 힘 방향 | −ĝ (모형은 +ĝ 가정) |
| 그리퍼 무게중심 방향 차이 | 99° |
| 토크 기준점 거리 | 187.7 mm |
| 파지 회전 차이 | 137.9° |
| 영점/측정 자세 차이 | 최대 174° |
