# 저울 총질량 + 토크 전용 추정

세션 `20260904_1736` 의 실패(잔차팽창 5,527~6,507 배, 부위 밀도가 하한 50 에
붙음, 추정 총질량 416.9 g vs 저울 571.0 g)에 대한 대응입니다.

---

## 무엇을 바꿨나 — 한 문단

**힘 3축을 추정에서 뺐습니다.** 대신 총질량을 저울에서 받습니다
(`--total-mass-kg`). 그리고 파지점 어긋남의 사전분포 중심을 0 에서
**FoundationPose 가 잰 값**으로 옮겼습니다 (`--grasp-mu-mm`).

측정·전송·기록은 6축 그대로입니다. 자르는 자리는 회귀행렬을 만드는 곳
한 군데뿐이라 원시 렌치가 보존되고 `|F| ~ M g` 검산도 계속 됩니다.

---

## 왜 힘을 빼도 되나 — 힘 블록의 계수는 1이다

`density_id_drake.regressor` 의 힘 행은 이렇게 생겼습니다.

```python
force_rows = FORCE_SIGN * G_ACC * np.outer(g_hat, VOLUMES)    # (3, P)
```

`np.outer(g_hat, VOLUMES)` 의 세 행은 각각 `ĝ[0]·V`, `ĝ[1]·V`, `ĝ[2]·V` —
**전부 같은 벡터 `VOLUMES` 의 상수배**입니다. 중력 방향을 3개로 늘려도,
100개로 늘려도 계수(rank)는 1 그대로입니다.

실측으로 확인한 값:

```
힘 행만 모은 행렬 (9, 5)   특이값 [0.0093  0  0  0  0]   -> rank 1
행공간 기저 vs VOLUMES 방향:  |cos| = 1.000000000000
```

즉 **힘 채널이 밀도에 대해 알려주는 것은 정확히 한 개, `Σ Vᵢρᵢ`(총질량)뿐**
입니다. 그 하나를 저울에서 받으면 힘 행은 더 줄 것이 없습니다.

## 그런데 그 힘 채널이 실물에서 가장 나쁘다

| 항목 | 값 |
|---|---|
| 상수 오프셋 `\|b\|` (세션 20260904_1736) | **58.7 N** |
| 램프 전체 무게 | 5.60 N |
| 코드의 `SIGMA_F` (백색화에 쓰는 값) | 0.10 N |

백색화가 힘 채널을 실제보다 **약 500 배** 신뢰하고 있었습니다.
`SIGMA_F` 를 58.7 로 올리면 힘 행의 가중치가 사실상 0 이 되므로, 아예 빼는
편이 정직하고 코드도 단순합니다.

저울(0.5 g)은 힘 채널의 58.7 N(≈ 6 kg)보다 **10⁴ 배** 정확합니다.

---

## 파지점 사전분포 — 5 mm 중심 0 이 railing 의 직접 원인

`GRASP_SIGMA_M = 0.005` (5 mm) 인데 실제 어긋남은 **173.9 mm** 였습니다.
35 시그마짜리 값을 MAP 이 받아들일 리가 없어서, 상자 한계 `±50 mm` 에
`[50, -50, 50]` 으로 세 축 전부 붙었습니다 — 부호까지 진단서와 일치합니다.

`grasp_columns` 자체는 처음부터 올바르게 구현돼 있었습니다.

```python
blocks.append(np.vstack([np.zeros((3, 3)),                 # 힘 행 = 0
                         FORCE_SIGN * M * G_ACC * skew]))  # 토크 행
```

기계장치는 다 있었고 **중심과 폭만 틀려 있었습니다.** 이제 사전평균을
인자로 받습니다 (`grasp_mu_m`), 상자도 그 둘레로 옮깁니다.

---

## 시뮬레이션 검증

5부위 물체, 파지점을 진단서의 173.9 mm 만큼 실제로 어긋뜨리고 복원:

| 설정 | 최대 밀도오차 | 총질량 오차 | 파지점 추정 [mm] |
|---|---|---|---|
| 6축, 파지점 명목 0 (예전 동작) | 205.27 % | 18.847 % | `[-11.3, 50.0, -50.0]` ← railing |
| 토크만, 파지점 명목 0 | 342.81 % | 26.126 % | `[-11.2, 50.0, -50.0]` ← railing |
| 6축 + FoundationPose 명목값 | 56.50 % | 0.034 % | `[-54.1, 82.0, -147.2]` |
| **토크만 + 명목값, 2라운드** | 57.12 % | **0.000 %** | `[-53.8, 82.1, -146.7]` |
| **토크만 + 명목값, 3라운드** | 56.66 % | **0.000 %** | `[-54.0, 82.1, -147.1]` |
| **토크만 + 명목값, 5라운드** | 56.63 % | **0.000 %** | `[-54.0, 82.0, -147.2]` |

(참 파지점 어긋남 `[-49.2, 82.0, -145.2] mm`)

읽는 법:

1. **명목값을 안 주면 railing 하고 총질량이 19~26 % 틀립니다.** 세션
   20260904_1736 의 지문과 같습니다.
2. **명목값을 주면 파지점이 5 mm 안으로 복원**되고 총질량 오차가 0 이 됩니다.
3. **토크만 써도 6축과 같습니다** (56.66 % vs 56.50 %). 오히려 총질량은
   더 정확합니다 (0.000 % vs 0.034 %).

남은 56 % 는 사전분포 수축입니다 — 추정기 결함이 아닙니다. 재분배 사전분포를
넓히면 양쪽 모두 같이 줄어듭니다:

| 재분배 sigma | 토크만 | 6축 |
|---|---|---|
| 2818 (평균밀도) | 56.66 % | 56.50 % |
| 8455 (×3) | 32.11 % | 31.29 % |
| 28185 (×10) | **5.53 %** | **3.99 %** |

---

## 쓰는 법

```bash
python dual_view.py --mode deploy --hardware real \
    --object desklamp --grasp pinch --grasp-part link_3 \
    --grasp-frame measured \
    --total-mass-kg 0.571 \
    --grasp-mu-mm -49.2 82.0 -145.2 \
    --grasp-sigma-mm 15 \
    --prior weight
```

| 인자 | 뜻 |
|---|---|
| `--total-mass-kg` | 저울로 잰 총질량 [kg]. **힌지까지 붙은 채로** 올린 값. 실물에서는 필수 |
| `--torque-only` / `--use-force` | 토크 전용 (실물 기본값) / 예전 6축 동작 |
| `--grasp-mu-mm X Y Z` | FoundationPose 가 잰 파지점 어긋남 명목값 [mm], 센서 프레임 기준 |
| `--grasp-sigma-mm` | 그 명목값의 불확실성. **10 이 아니라 15~20** 을 권합니다 (아래) |

`pivot_ui` 로 돌릴 때는 설정 파일에 넣습니다.

```
TOTAL_MASS_KG=0.571
GRASP_MU_MM=-49.2 82.0 -145.2
GRASP_SIGMA_MM=15
# USE_FORCE=1        # 예전 6축 동작으로 되돌릴 때만
```

### 안전장치

- `--hardware real` 인데 `--total-mass-kg` 가 없으면 **시작을 거부**합니다.
  없으면 자산 GT 로 총질량을 만들게 되어 "정답을 넣고 정답을 맞히는"
  실험이 됩니다.
- 저울 값을 받으면 총질량을 **고정**합니다. 예전에는 라운드마다
  `V @ rho_hat` 로 다시 계산해서, 그 추정 오차가 파지점 열의 계수로
  되먹임되어 두 미지수가 서로를 흉내냈습니다.
- `pivot_ui` 의 `--prior water` 하드코딩을 `weight` 로 바꿨습니다.
  `water` 는 등방 사전분포라 총질량을 전혀 안 묶습니다.

---

## 남은 문제 — 이 변경으로 **안** 고쳐지는 것

이 패치는 힘 채널과 파지점 사전분포만 다룹니다. 아래는 그대로입니다.

### 1. 각도 오차 3° 가 이제 지배항이다 ★최우선★

`--angle-error 0.03` 은 `dual_view.py` 에서 `sigma_deg = 0.03 * 100 = 3.0도`
로 들어갑니다.

- 위치 오차 10 mm 는 **공통 모드**입니다. 모든 도심을 똑같이 밀어내므로
  `δ` 가 흡수하고, 힌지각을 여러 개 쓰면 추정 가능합니다.
- 각도 오차 3° 는 **차동**입니다. 말단 링크만 움직입니다. `δ` 가 못
  흡수하고 밀도 분리 정보로 직행합니다.

```
말단 도심 이동 = (힌지~도심 거리) x sin(3°)
    0.15 m -> 7.9 mm      0.25 m -> 13.1 mm (힌지 2개를 거치는 말단)
```

**정확한 값은 이미 있는 도구로 뽑으십시오** — 추정하지 마시고:

```python
J  = dc.angle_jacobian(theta, rho_gt, dc.CANONICAL_TRIAD)
dy = J @ np.deg2rad(np.full(n_joint, 3.0))
```

5 % 목표를 지키려면 각도가 **1.5~2° 이하**여야 할 것으로 봅니다.
가능하면 힌지각을 거치지 말고 **FoundationPose 의 링크별 6D 자세에서
`cᵢ` 를 직접** 뽑으십시오. 부호 문제도 `joint_2_3` 의 비단조 접힘도
존재 자체가 사라집니다.

### 2. 손-눈 캘리브레이션 오차가 10 mm 에 더해진다

FoundationPose 는 **카메라 기준** 자세를 줍니다. 필요한 것은 AFT200 센서
프레임 기준입니다. 그 사이 변환의 오차(보통 위치 2~5 mm, 자세 0.5~1°)가
더해집니다. 자세 1° 는 팔 뻗은 거리 0.5 m 에서 8.7 mm 입니다.

**그래서 `--grasp-sigma-mm` 을 10 이 아니라 15~20 으로 두십시오.**
가정하지 말고, 알려진 위치를 여러 자세에서 보고 재현 산포를 재십시오.

### 3. 메시 프레임 규약 불일치 ★173.9 mm 를 그대로 재현할 수 있는 함정★

FoundationPose 는 **자기가 받은 메시의 원점** 기준으로 자세를 냅니다.
회귀행렬은 **Drake `KIN_PLANT` 바디 프레임** 기준으로 `cᵢ` 를 냅니다.
이 둘이 같다는 보장이 없습니다. 173.9 mm 가 정확히 이 종류의 버그였습니다.

**수치로 검증하십시오.** 알려진 자세에 물체를 놓고 양쪽이 준 `cᵢ` 를
직접 찍어 비교. 가정하고 넘어가면 같은 실패를 반복합니다.

### 4. FoundationPose 오차는 무작위가 아니라 계통적이다

`effective_cov` 는 각도 오차를 `R_eff = R_sensor + J Σ_θ Jᵀ` 에
**잡음처럼** 흡수합니다 — 라운드를 늘리면 줄어든다는 뜻입니다.
그런데 같은 장면을 같은 시점에서 보면 같은 값으로 틀립니다.

`bias_covariance` 가 이를 위해 있는데 실물 경로에서 호출되지 않습니다
(`study_stopping.py` 에서만). 이것이 "보고 반폭"이 실제 오차와 따로 논
이유일 수 있습니다.

대응: `--stop-rule bias` 를 쓰고, **라운드마다 힌지각을 실제로 바꿔서**
오차를 탈상관시키십시오.

### 5. 힌지각을 여러 개 써야 `δ` 와 `ρ` 가 분리된다 ★설계 제약★

토크 방정식을 정리하면 이렇습니다.

```
τ = g·skew(ĝ) · [ M·δ  −  s ],       s = Σ Vᵢρᵢcᵢ
```

토크는 `M·δ` 와 `s` 를 **이 조합으로만** 봅니다. 중력 3방향은 같은
`[Mδ − s]` 를 세 각도에서 볼 뿐이라 정보가 3개입니다. 미지수는
`δ` 3 + `ρ` P 개입니다.

빠져나갈 길은 하나 — **`δ` 는 고정이지만 `s(θ)` 는 힌지각에 따라 변합니다.**

```
3J + 1 >= P + 3        P=3 이면 J >= 2,   P=5 이면 J >= 3
```

**함정**: 물체를 강체로 쥐고 있으면 **로봇 팔 자세를 아무리 바꿔도 센서
프레임에서 `cᵢ` 는 안 움직입니다.** 로봇 자세는 `ĝ` 만 바꿉니다.
`cᵢ` 를 움직이는 건 오직 **램프 자신의 힌지각**입니다.

### 6. 타어 색인이 `ĝ` 인데 케이블 당김은 `q` 의 함수다

`TareTable` 은 중력 방향으로 키를 겁니다. 같은 `ĝ` 를 만드는 관절각은
무수히 많고(6축에 제약 2개 → 4차원 널공간), 케이블 당김은 팔 형상 전체의
함수입니다. **`ĝ` 의 함수가 아닌 것을 `ĝ` 로 색인**하고 있습니다.
모형으로 못 맞춘 4.35 N 잔차가 이것으로 설명됩니다.

고치려면 타어할 때의 `q` 6개를 저장하고, 측정 때 **IK 를 다시 풀지 말고**
그 `q` 를 관절 명령으로 재현해야 합니다 (허용 0.05°).

### 7. 그 밖에

- **원시 렌치를 매 라운드 저장하십시오.** 세션 기록에
  `raw_wrench_recovery.available: false` 로 남아 있고, 이것 없이는
  어떤 것도 오프라인에서 재검증할 수 없습니다.
- `TimedTare` 만료검사가 무력화돼 있습니다 (`dual_view._load_tare` 가
  **로드 시점**에 `record()` 를 불러 30분 검사가 항상 통과). 세션은
  69시간 된 타어를 썼습니다.
- `preflight.json` 의 "각도 잔차 최대 0.00 deg" 는 40초 스윕에서 나올 수
  없는 값입니다. 캘리브레이션이 실제로 안 돌았을 가능성이 있습니다.
- 어느 `desk_lamp.py` 가 돌았는지 확정하십시오. pinch 점 vs 부피 도심이
  105.8 mm 다르고, 이것이 `--grasp-mu-mm` 값 자체를 정합니다.
  `grep -c 'geometry\[root\]\["centroid"\]' <checkout>/my_work/desk_lamp.py`

---

## 부수적으로 고친 버그

`R_EPS_DIAG` 를 **세 곳**에서 각자 `[f]*3 + [t]*3` 으로 만들고 있었습니다
(`density_id_drake`, `density_id_objects.set_sensor_averaging`,
`dual_view` 의 예보 루프). 토크 전용으로 바꾸면 뒤의 두 곳이 6축으로
되돌려 놓아서 `sensor_cov` 가 `(18,18)`, 야코비안이 `(9,9)` 로 어긋나
`effective_cov` 에서 터졌습니다.

`alg.noise_diag()` / `alg.rebuild_noise()` 한 곳으로 모았습니다.
`R_EPS_DIAG` / `R_STACK_DIAG` / `W_HALF` 는 항상 같이 만들어집니다 —
하나만 갱신하면 백색화 가중치와 회귀행렬의 행 수가 어긋나 **조용히**
틀린 답이 나옵니다.
