# 바뀐 것들

팀원이 `git pull` 한 뒤 무엇이 달라졌는지 보는 곳입니다.
새로 받는 분은 [SETUP.md](SETUP.md) 부터 보세요.

---

## 2026-08-31 — 그리퍼를 화면에서 몰고, 파지점을 눈으로 맞추고, 결과를 색으로 본다

실물 세션의 UI 를 세 토막으로 정리했습니다. 순서는 **파지 → 관절각(반복) →
결과 비교** 입니다.

**① 그리퍼가 소프트웨어로 움직입니다** — `my_work/gripper_hw.py` (새 파일).

Robotiq 2F-85 를 FTDI/Modbus-RTU 로 직접 몹니다. pyserial 을 안 씁니다
(termios 로 충분하고, 부트스트랩에 꾸러미를 더하지 않으려고).

```bash
$R python gripper_hw.py --check                       # 장비 없이 프레임 검증
$R python gripper_hw.py --port /dev/ttyUSB0 --demo    # 활짝 / 절반 / 닫힘
```

쓰는 쪽에서 알아야 할 것은 `activate()` `set_opening(m)` `open()` `close()`
`status()` 다섯 개뿐입니다. 상태에서 **gOBJ** 를 풀어 "물었다" 를 돌려줍니다 —
빈 손으로 8 초짜리 준정적 이동을 하는 일이 없어집니다.

**② 파지 단계가 글자에서 그림 + 버튼으로 바뀌었습니다** — `dual_view.await_grasp`.

예전에는 터미널에 "그리퍼를 열고 물체를 넣으세요" 라고 **적혀만** 있었고,
사람이 별도 스크립트로 여닫았습니다. 그러면 개구량이 계획과 달라지고,
개구량이 달라지면 물체가 죠 안에서 다른 자리에 앉습니다. 그게 곧 파지점이
어긋나는 것이고, 파지점은 회귀행렬의 모든 모멘트팔입니다
(`study_grasp.py`: 2 mm 에 밀도 오차 113 %).

이제 작업자 화면에 **그리퍼 기준 좌표계**로 파지 목표를 그립니다. 씬이
용접해 둔 그대로의 물체, 빨간 점(파지점 = 센서 프레임 원점), 검은 두 장(죠)
이고, 죠는 실제 개구량을 따라 움직입니다. 버튼은 다섯 개입니다.

```
① 활짝 열기      ② 계획 개구 XX.X mm 로 물기      ③ 파지 완료
조금 열기/닫기 (±2 mm)   ·   개구 슬라이더 + '슬라이더 값으로 이동'
```

③ 에는 관문 둘을 뒀습니다. **무는 명령을 준 적이 없으면** 넘어가지 않고,
**gOBJ 가 물림을 보고하지 않거나 개구량이 계획과 2 mm 넘게 다르면** 경고한 뒤
한 번 더 눌러야 넘어갑니다 (사람 판단을 막지는 않되, 모르고 지나치지 않게).

그리퍼가 없거나 포트를 못 열면 예전 흐름(손으로 여닫고 버튼 하나)으로
그대로 떨어집니다. `--gripper-port`, `--no-gripper` 로 고릅니다.

**②-b 파지력을 205 N 으로 올리고, 세션 중에 계속 만질 수 있게 했습니다.**

원본 스크립트의 rFR 32 (약 47 N) 를 그대로 쓰고 있었습니다. 집어 올리기에는
충분해도 이 실험에는 모자랍니다. **미끄러지는 것이 문제가 아니라 죠 안에서
돌아가는 것이 문제**이기 때문입니다 — 손목을 중력 3방향으로 돌릴 때마다
무게중심이 파지점에서 떨어진 만큼 모멘트가 걸립니다. 라운드마다 조금씩
돌아가면 파지점 어긋남 delta 가 상수가 아니게 되고, 그러면 `grasp_map` 이
그걸 **원리상 못 풉니다** (delta 를 모든 라운드 공통 미지수로 놓고 풀기 때문).

기본값 rFR 220 = 약 205 N. `--gripper-force <N>` 으로 시작값을 주고,
세션 중에는 작업자 화면의 **`파지력 [N]` 슬라이더 + '슬라이더 힘으로 다시 물기'**
로 계속 바꿉니다. 명령할 때마다 힘(N)과 무게 대비 미끄럼 여유를 찍습니다.
얇은 플라스틱처럼 눌리는 물체는 **낮추세요** — 형상이 눌리면 스캔 메시와
실물이 달라져 부피가 틀립니다.

장비 앞에서 값만 맞춰 보려면:

```bash
$R python gripper_hw.py --port /dev/ttyUSB0 --grip-mm 44 --force-n 120
```

**②-c 위치만 명령해서는 물리지 않던 것을 고쳤습니다.**

물체 단면과 **똑같은** 개구를 명령하면 죠가 그 자리에 도달해 버리고
(gOBJ=3 '요청 위치 도달') 힘이 하나도 안 걸립니다. 새 `grip()` 은
`CLAMP_SQUEEZE_M = 4 mm` 만큼 더 좁게 명령해 물체에 **걸려서** 멈추게 하고
(gOBJ=2), 그때부터 rFR 만큼 조입니다. 되읽은 개구량은 물체 단면 그대로라
계획값 대조는 그대로 됩니다.

**②-f 그리퍼를 키보드로 몹니다 (스페이스바 토글 대신).**

원본 `robotiq_keyboard_toggle.py` 는 스페이스바 하나로 열기/닫기만 했습니다.
그걸로는 **이 물체에 얼마가 맞는지** 를 못 찾습니다. 개구와 힘을 각각 따로,
키를 **누르고 있는 동안 연속으로** 바꾸게 했습니다.

```
  a / <-  열기 (누르고 있으면 계속)      w / ^  파지력 +
  d / ->  닫기 (누르고 있으면 계속)      s / v  파지력 -
  o  활짝 열기      c  꽉 닫기      g  계획 개구로 물기
  Enter  확정      q  나가기
```

터미널에는 '키를 뗐다' 는 신호가 없습니다. 대신 누르고 있으면 OS 자동 반복이
같은 글자를 초당 수십 번 보내므로, 한 번 들어올 때마다 1.5 mm / 4 카운트씩
움직이게 했습니다. 한 번에 몰려 들어온 키는 `fold_keys()` 가 **하나의 목표로
접어서 한 번만 명령**합니다 — 글자마다 명령하면 도착을 기다리는 사이에 입력이
쌓여 조작이 끈적해집니다 (그래서 `wait=False` 로 보냅니다).

두 군데서 씁니다.

```bash
# 1) 단독 — 값만 맞춰 볼 때
$R python gripper_hw.py --port /dev/ttyUSB0 --keyboard --plan-mm 44

# 2) 세션의 파지 단계 — 터미널에서 키보드가 그대로 먹습니다.
#    Meshcat 버튼도 그대로 살아 있어 둘 다 씁니다.
```

로그로 넘겨 돌리면(stdin 이 tty 가 아니면) 조용히 꺼지고 버튼만 남습니다.

**②-d 실물에서 관절각을 슬라이더로 안 받고 FoundationPose 로 읽습니다.**

`adjust_manually` 는 통과 판정을 **슬라이더 값**으로 하고 있었습니다. 실물에서
슬라이더는 아무것도 안 움직입니다 — 작업자가 손으로 관절을 돌린 뒤 "몇 도로
맞췄다"를 눈대중으로 입력하는 칸이 될 뿐입니다. 그런데 추정식에 들어가는
값은 `measure_angles()` 가 읽은 FoundationPose 값입니다. 즉 **통과한 각도와
실제로 쓰이는 각도가 서로 다른 값**이었습니다.

(`operator_ui.ANGLE_TOL_DEG` 주석은 "판정은 FoundationPose 가 읽은 각도로
한다(dual_view.adjust_manually)" 고 적혀 있었는데, 코드는 그렇지 않았습니다.
주석도 같이 고쳤습니다.)

새 `adjust_by_pose` 는 `pose_sensor` 가 있으면 자동으로 그쪽으로 갑니다.

```
슬라이더 없음. 작업자는 물체를 손으로 돌리기만 한다
트래커가 읽은 각도로 화면의 물체가 실시간으로 따라 움직인다
허용 오차 안에 들면 신호등이 초록으로 바뀐다
① 의 통과 판정도 트래커 값 — 잡음에 뒤집히지 않게 5샘플 중앙값을 쓴다
트래커가 죽으면 통과하지 않고 2초마다 이유를 알린다 (가장 위험한 고장)
```

**②-e 모의 장비에도 자세 센서를 붙였습니다.**

`simulated_hardware()` 가 `pose=None` 을 주고 있어서, `--hardware sim`
리허설은 슬라이더 경로로 가고 실물만 트래커 경로로 갔습니다. **리허설이
실물과 다른 코드를 밟고 있었습니다.** 이제 모의 트래커를 붙이고, 리허설에서는
슬라이더가 '실물 관절' 자리를 대신합니다 (`... [deg] (리허설: 실물 관절 대신)`).
두 경우가 같은 함수를 지나갑니다.

**③ 탐색이 끝나면 밀도 비교 화면이 뜹니다** — `my_work/density_view.py` (새 파일).

같은 물체를 나란히 그리고 부위별 밀도를 **turbo(무지개)** 로 칠합니다.

```
[초기]  저울 총무게만 아는 상태 — 모든 부위가 한 색
[추정]  탐색 결과 — 부위마다 색이 갈린다
[정답]  시뮬레이션에서만. 채점용이며 탐색·정지 판단에는 안 쓴다
```

컬러맵과 정의역은 `figures/desklamp_density_render.py` 가 논문 그림에 쓴 것과
같습니다. `--no-density-view` 로 끕니다.

**④ `--grasp pinch` 를 고를 수 있게 했습니다.**

`desk_lamp.DEFAULT_GRASP` 는 `"pinch"` 인데 `dual_view`/`robot_node` 의
`--grasp` 는 선택지가 `centroid|base_frame` 뿐이라 **볼록 조각에서 고른
'실제로 물리는 자리' 를 아예 쓸 수 없었습니다.** 기본값은 그대로
`centroid` 입니다.

---

## 2026-08-14 (6) — 실물 준비: 여유 1 cm, 물성 주입 URDF, 수동 파지

실물 실험을 앞두고 네 가지를 고쳤습니다. 전부 계측했습니다.

**① 충돌 여유를 6 mm → 10 mm 로 올렸습니다.**

시뮬레이션이 재는 간격은 '모형 사이' 간격입니다. 실물에는 손-눈 보정 잔차,
FoundationPose 자세 오차, 파지점 어긋남(5 mm), 스캔 형상 오차가 겹쳐 쌓입니다.

그런데 **10 mm 를 요구해도 9 mm 까지 파고들 수 있었습니다.** 경로계획기가
문턱을 10% 낮춰 잡고 있었기 때문입니다(`PLANNER_MARGIN_RATIO = 0.9`).
IK 가 제약을 경계에 딱 붙여 풀어서, 같은 문턱을 쓰면 시작·목표 자세가
부동소수점 차이로 탈락하는 것을 그렇게 피하고 있었습니다.

**여유는 지켜야 하는 쪽을 깎는 게 아니라 만드는 쪽을 높여서 줍니다.**
이제 IK 만 `IK_SLACK_M = 1 mm` 더 요구하고, 계획기는 문턱을 그대로 지킵니다.

```
                 후보 자세      경로 위 실제 최소 간격
3-link   6 mm     23/25              7.00 mm
3-link  10 mm     23/25             10.89 mm   <- 후보 손실 없음
램프     6 mm     24/25
램프    10 mm     19/25             10.04 mm   <- 5개 줄어듦
```

램프는 후보 5개를 잃습니다. 2F-85 가 커서 테이블·팔과 더 자주 걸리기
때문입니다. 19개면 탐색에 충분합니다.

**② URDF 산출물이 배달물 원본에 물성만 채워 넣습니다.**

예전에는 산출물을 **새로 지었습니다.** 그래서 스캔 메시가 AABB 상자로
퇴화하고, 파지 부위를 뿌리로 다시 세운 트리가 나가고, 배달물의 볼록분해
21조각이 통째로 버려졌습니다. 램프 Arm 은 AABB 가 실제 재료의 3배라
시뮬레이터에서 굵기가 3배인 팔이 됐습니다.

배달물의 `<inertial>` 은 원래 **밀도 1000 kg/m³ 자리표시자**였습니다
(질량이 부피 그대로: link_1 441.02 cm³ → 0.4410195 kg). 그러니 할 일은
새로 짓는 게 아니라 그 자리표시자를 바꾸는 것이었습니다.

```
outputs/estimated_desklamp.urdf
  = assets/desk_lamp_minimal_sim/drake/object.urdf 그대로
  + 링크 3개의 질량 · 무게중심 · 관성텐서만 교체
  + 메시 경로 24개를 outputs/ 기준 상대경로로 재작성
```

형상·관절·트리·충돌메시는 한 글자도 안 바뀝니다. 배달물 폴더도 안 건드립니다.

관성텐서도 고쳤습니다. `Part.inertia_unit` 에 메시에서 뽑은 관성이 이미
들어 있었는데 export 가 그걸 안 쓰고 직육면체 공식을 쓰고 있었습니다.
상자 공식은 관성적(慣性積)을 0 으로 놓는데, 굽은 팔은 링크 축에 정렬돼
있지 않아 실제로 0 이 아닙니다. Drake 로 되읽어 검증하면 오차 1e-10 입니다.

**③ 각도 허용 오차 3° → 5°, 후보 검사 창도 같이 넓혔습니다.**

조정 통과 판정은 **FoundationPose 가 읽은 각도**로 합니다. 그러니 이 창이
FoundationPose 자체 오차보다 좁으면 작업자가 아무리 잘 맞춰도 통과할 수
없고 라운드가 무한히 반복됩니다.

창을 넓히면 **실제 각도가 목표에서 그만큼 벗어난 채로 로봇이 움직입니다.**
그래서 `angle_probes` 가 훑는 범위도 `1.96σ` 에서 `1.96σ + 허용오차` 로
넓혔습니다. 한쪽만 고치면 검사한 적 없는 형상으로 로봇이 이동합니다.
(시뮬레이션에서는 각도가 목표에 정확히 맞춰져서 이 구멍이 안 보입니다)

**④ 수동 파지 — 시작 자세로 자동 이동하기 전에 물체를 물립니다.**

실물에서 작업자가 직접 물체를 물릴 것이므로 확인해 보니 두 군데가 틀렸습니다.

- `begin()` 이 팔을 **시작 자세에 있다고 가정**했습니다(`set_arm_from`).
  실물은 전원 켠 자리에 서 있습니다. 그러면 첫 이동에서 RRT 가
  start_q → start_q 를 풀어 경유점 하나를 내놓고, 드라이버는 그 한 점으로
  팔을 **계획에 없는 직선으로** 보냅니다. 이제 `sync_from_robot()` 이
  `driver.joint_positions()` 로 실제 자세를 읽습니다.
- 파지 단계 자체가 없었습니다 (시뮬레이션은 물체가 그리퍼에 용접돼 있음).
  이제 `await_grasp()` 가 서보를 끄고 → 작업자가 물리고 → 버튼 확인 →
  서보를 켜고 → **자세를 다시 읽고**(꺼진 동안 처짐) → 여유를 확인한 뒤
  시작 자세로 이동합니다. 파지가 이동보다 먼저여야 합니다. 경로계획의
  충돌 모형이 '물체를 든 팔' 이기 때문입니다.

`hardware.RobotDriver` 에 `servo_on()` 을 더했습니다. 끄기만 하는
인터페이스는 덫입니다 — 물린 뒤 다시 켤 방법이 없습니다.

**⑤ my_work/ 를 정리했습니다.** 파이썬 import 는 하나도 안 건드렸으므로
기존 실행 명령이 전부 그대로 동작합니다.

```
figures/   study_*.png 등 그림 11개
outputs/   estimated_*.urdf, plan_*.json, results_*.txt  (+ README)
scratch/   _*.py 6개, template.py, adaptive_select.py    (+ README)
```

산출물이 다시 흩어지지 않도록 저장 경로도 함께 옮겼습니다
(`--plot` 기본값, `savefig`, URDF 기본 출력).

**⑥ PC 는 한 대면 됩니다.** `robot_node.py` 도 pydrake 를 쓰고
`prepare()` 를 돌리므로 "환경이 달라서 나눈다" 는 이유가 성립하지 않습니다.
자세한 것은 [VERIFICATION.md](my_work/VERIFICATION.md) 2장.

---

## 2026-08-14 (5) — 순간이동·헛도는 죠·이동 중 충돌 (중요)

셋 다 실제 버그였고, 전부 계측해서 찾았습니다.

**① 경로가 거꾸로 나오고 있었습니다.** RRT-Connect는 두 트리를 번갈아
키우느라 매 반복마다 a와 b를 맞바꿉니다. 그래서 연결이 성사된 순간의
tree_a가 출발 쪽이 아닐 수 있는데, 그걸 안 따지고 이어 붙이고 있었습니다.

```
[진단] 경로가 어긋납니다: path[0]-start 134.2 deg, path[-1]-target 134.2 deg
                          └ 두 값이 같다 = 경로가 통째로 뒤집혔다는 뜻
```

로봇은 **목표 자세로 순간이동한 뒤 거꾸로 되짚어 와서 출발점에 멈췄습니다.**
즉 각도 측정 자세에 애초에 도착하지 못했습니다. 지금은 0/44 경로가 뒤집힙니다.

**② 죠가 헛돌고 있었습니다.** 개구 각도를 URDF의 rpy에 구워 넣을 때 부호가
반대였습니다. **오므리라고 할수록 벌어졌습니다.**

```
요청 19.4 mm -> 실제 116.5 mm      요청 44.0 mm -> 실제 105.1 mm
```

각도를 축 회전으로 제대로 곱하도록 고쳤고, 이제 요청값과 실제 간격이
소수점까지 일치합니다. 개구량은 볼록 껍질이 아니라 **화면에 보이는 표면**
에서 재므로 패드가 물체에 닿습니다.

**③ 각도 측정 이동을 지난 라운드 형상으로 계획했습니다.** `plan_path`가
쓰는 `believed_q_deg`가 조정 직후에는 아직 갱신되지 않아서, 화면의 물체는
새 각도인데 경로는 옛 각도 기준이었습니다.

**감시 두 개를 붙였습니다** (실물에서도 쓸모가 있습니다).

```
publish()   프레임 사이 관절이 5도 넘게 튀면 경고 — 순간이동 감지
move_to()   이동 중 최소 간격을 재서 파고들면 몸체 쌍까지 출력
```

고친 뒤: 순간이동 경고 0건, 경로 어긋남 0건, 프레임 최대 0.54 deg,
이동 중 최소 간격 5.5~6.0 mm.

---

## 2026-08-14 (4) — 램프 배달물 0814 판으로 교체

협업자가 준 새 배달물(`assets/desk_lamp_minimal_sim`)이 **우리가 손으로
만들던 것을 직접** 줍니다.

```
drake/collisions/link_N/part_XX.obj    볼록 분해 (link_1 10, link_2 5, link_3 6조각)
drake/visuals/link_N_*_rgb.gltf        정점 색(COLOR_0)이 든 glTF
geometry/parts/*_watertight_rgb.ply    닫힌 메시 (부피·도심용)
```

- **색 조각으로 쪼개던 것을 그만뒀습니다.** glTF의 COLOR_0을 Meshcat이 그대로
  그립니다. 부피·도심은 예전 배달물과 소수점까지 같습니다(441.02 / 165.70 /
  219.58 cm³) — 같은 물체의 포장만 좋아진 것입니다.
- **볼록 분해를 배달물에서 씁니다.** 예전에는 삭제된 배달물에서 구운 캐시를
  재활용하고 있었습니다.

**glTF 화면 메시가 90° 돌아가 붙던 것을 고쳤습니다.** 링크가 따로 놀아
분해된 것처럼 보였습니다. glTF 규약은 **y가 위**라서 Drake가 그 규약대로
x축 +90°를 돌려 읽는데, 배달물은 .ply/.obj와 같은 **z-up**으로 만들어져
있습니다. 같은 부위를 glTF와 OBJ로 각각 그려 화면 위치를 재서 확인했고
(x축 −90°를 주면 화소 위치·깊이까지 겹칩니다), 그 보정을 넣었습니다.
충돌 형상은 처음부터 제대로 붙어 있었습니다(관절에서 거리 0.0 mm) —
화면만 틀렸던 것입니다.

**파지점을 도심이 아니라 '실제로 물리는 자리'로 바꿨습니다.** 굽은 팔의
도심은 재료 안에 있어도 그 자리 단면의 중심이 아니라서, 죠를 오므리면 한쪽
패드가 10.5 mm 허공을 물었습니다. 이제 볼록 조각(팔을 토막 낸 것이라 거의
원기둥)에서 중심·최단축·최장축을 뽑아 씁니다.

```
link_3 part_00:  파지점 [0.254 0.409 0.088],  물 폭 17.6 mm
개구 17.6 mm -> 패드 x = ±8.8 mm,  파지점 근처 재료 −8.3 ~ +9.3 mm  (맞물림)
```

굽은 팔의 파지 방향은 좌표축에 나란하지 않으므로 `Part.grasp_axis` 가
축 번호 대신 **단위벡터**도 받습니다.

재검증: 램프 후보 20/25, 2 라운드, 오차 0.00~0.18%. 3-link 23/25, 2-link 5/5.

---

## 2026-08-14 (3) — 그리퍼가 실제로 물체를 뭅니다 (중요)

화면에서 물체가 손가락 위에 **떠 있는 것처럼** 보인다는 지적이 있었고,
재보니 실제로 두 군데가 틀려 있었습니다.

**① 파지점 높이가 39 mm 틀렸습니다.**
Robotiq의 TCP 높이를 손가락 링크의 **몸체 원점**에서 읽고 있었는데, 이 URDF는
원점이 패드 면에서 39 mm 아래에 있습니다. 그래서 물체가 패드보다 그만큼
안쪽(손목 쪽)에 붙었습니다. 이제 **패드 면의 중심**을 직접 재서 씁니다.

```
개구 44 mm 에서   예전 TCP z 123.8 mm  ->  고친 값 145.3 mm
                  패드 면은 z 123~168 mm 에 있으므로,
                  44 mm 물체는 z 123~167 을 차지해 딱 들어맞습니다.
```

**② 죠가 물체의 엉뚱한 축을 물고 있었습니다.**
물체를 그리퍼에 붙일 때 x축 롤만 주고 있어서, 물체의 x축이 **죠가 열리는
방향**에 그대로 남았습니다. 3-link는 그 축이 150 mm 길이 방향이라, 44 mm로
오므린 패드가 물체 옆구리에 걸렸습니다.

이제 물체의 **가장 좁은 단면 축**을 죠 방향(그리퍼 x)으로, **길이 축**을
손목 축(그리퍼 z)으로 보냅니다. 스캔 물체는 메시로 잰 단면이 어느 축인지도
함께 넘깁니다(`Part.grasp_axis`).

길이 축을 어디로 보낼지는 재서 정했습니다.

| 길이 축 | 3-link | 2-link | 데스크 램프 |
| --- | --- | --- | --- |
| 손목 축(z) — **채택** | 23/25 | 5/5 | 20/25 |
| 죠를 가로질러(y) | 시작 자세 없음 | 3/5 | 4/25 |

확인은 숫자로 했습니다. 개구 44 mm에서 패드 면은 x = +22.3 / −21.7 mm,
물체 반폭은 22.0 mm — **틈 0.3 mm**로 맞물립니다.

> 참고: 이 파이프라인은 파지를 **접촉으로 풀지 않습니다.** 물체를 파지점에
> 용접해 붙이고, 죠는 물체 단면에 맞춰 오므린 모양으로 굳힙니다. 정지 상태의
> 중력 렌치만 쓰므로 접촉력이 필요 없기 때문입니다. 그래도 위 두 값이
> 틀리면 충돌 판정과 도달 판정이 실제와 어긋나므로 고쳐야 했습니다.

**③ 이동 속도를 시간이 아니라 속도로 묶습니다.**
예전에는 이동 시간이 고정이라 경로가 길수록 빨라졌습니다. 이제 관절 속도가
`--max-joint-speed-deg`(기본 15 deg/s)를 넘으면 시간을 늘립니다. 8초 이동에서
관성 토크는 중력 토크의 3.5%였고, 4초면 14%, 2초면 68%입니다.

---

## 2026-08-14 (2) — 파지점 어긋남을 함께 풉니다

**무엇이 문제였나.** 시뮬레이션에서는 "센서 원점이 물체의 이 점에 있다"고
정확히 줄 수 있지만 실물에서는 못 줍니다. 지그를 대도 몇 mm는 어긋나고,
그 어긋남은 **모든 부위의 모멘트팔을 똑같이 밀어 놓습니다.** 잡음이 아니라
치우침이라 라운드를 늘려도 안 없어집니다.

측정해 보니 이것이 지금까지 본 것 중 **가장 큰 오차원**이었습니다.
(3-link, D-최적 6라운드, `study_grasp.py`)

| 파지점 어긋남 | 밀도만 품 | 밀도 + 파지점 함께 품 |
| ---: | ---: | ---: |
| 0 mm | 0.40% | 0.32% |
| 2 mm | 112.62% | 0.32% |
| 5 mm | 209.87% | 0.32% |
| 10 mm | 339.17% | 0.32% |

**어떻게 고쳤나.** 어긋남 δ는 미지수 3개로 **모형 안에** 들어옵니다.

```
센서 원점에서 잰 토크 = (모형의 토크) + M·G·(g × δ)
                                        └ M(총질량)은 저울로 이미 안다
```

힘 성분에는 안 나타나고, 중력 3방향을 다 쓰면 δ의 세 성분이 다 잡힙니다.
저울 총무게가 10% 틀려도 밀도 오차는 그대로였습니다(δ 추정치만 0.46 mm 밀림)
— 실제로 결정되는 것은 M·δ의 곱이기 때문입니다.

**전체 파이프라인에서** (3-link에 5 mm를 일부러 어긋뜨리고):

```
끄면   link0 3.14%  link1 0.52%  link2 8.03%   (힌지 파라미터가 치우침을 흡수)
켜면   link0 0.21%  link1 0.96%  link2 0.20%   δ 추정 4.85 mm / 실제 5.0 mm
```

**대가는 불확실성입니다.** 미지수가 3개 늘어 사후 반폭이 넓어집니다. 위
실행은 8라운드를 돌고도 목표 1%를 "주장"하지는 못했습니다(10.4%). 점 추정은
정확하지만 알고리즘이 그렇다고 말할 근거가 아직 부족한 것입니다. 지그로
어긋남을 줄이면(사전분포를 좁히면) 그만큼 빨라집니다.

```bash
--grasp-sigma-mm 5     # 파지점 사전분포. deploy 기본 5, sim 기본 0
--grasp-error-mm 5     # 시뮬레이션에서 일부러 어긋뜨리기 (검증용)
```

- 새 파일: `my_work/study_grasp.py`, `my_work/study_grasp.png`
- 새 함수: `design_core.grasp_columns`, `grasp_map`; `tls_map(grasp_sigma_m=...)`
- 같이 고친 것: 잔차 팽창 계산이 파지점 몫을 예측에 안 넣어 팽창이 ×105까지
  뛰고 정지 조건이 영영 만족되지 않던 문제 (지금은 ×1.27)

**TLS와 같이 쓸 때 주의.** 파지점을 찬 시작(0)으로 TLS에 넣으면 각도 보정과
서로를 흉내내며 국소해에 빠집니다 — 어긋남이 없는데도 9 mm를 지어내고 오차가
110%까지 났습니다. 그래서 항상 선형 해(`grasp_map`)를 먼저 풀어 그 값을
시작점으로 넘깁니다. 코드가 자동으로 그렇게 합니다.

---

## 2026-08-14

### 관절각을 관절마다 자기 자리에서 읽습니다 (동작이 바뀝니다)

**무엇이 문제였나.** 각도는 카메라(FoundationPose)가 읽는데, **관절 축이
시선과 직각이면 회전이 화면 밖(깊이 방향)으로 나가 거의 안 보입니다.**
예전에는 물체를 잡은 자세 하나에서 모든 관절 각도를 읽었는데, 3-link의
joint2는 그 자세에서 0.37 px/deg 밖에 안 나왔습니다. 1도와 3도를 구별하기
어려운 수준입니다.

**무엇이 바뀌었나.** 관절마다 그 관절이 가장 잘 보이는 자세를 따로 만들고,
각 관절의 각도를 자기 자세에서 읽습니다. 축이 나란한 관절끼리는 한 자세로
묶습니다.

**얼마나 좋아지나.** 3-link를 같은 seed로 켜고/끄고 4번씩 돌린 결과입니다.

| seed | 라운드 (켬/끔) | 부위 최대오차 (켬/끔) | 각도오차 (켬/끔) |
| --- | --- | --- | --- |
| 0 | 2 / 2 | 0.040% / 0.040% | 0.16° / 0.47° |
| 1 | 2 / 3 | 0.030% / 0.070% | 2.24° / 2.40° |
| 2 | 4 / 6 | 0.070% / 0.130% | 1.94° / 2.89° |
| 3 | 2 / 6 | 0.050% / 0.240% | 1.63° / 4.34° |

평균 최대오차 **0.048% vs 0.120%**, 라운드 **2.5 vs 4.25회**.

**어느 방향이 좋은지는 재서 정했습니다.** `my_work/study_startpose.py` 가
D456을 실험실 배치대로 세우고 물체를 돌려가며, 관절 1도당 화면 변화를 두
가지 방법으로 잽니다. 축이 시선과 직각인 자세가 가장 나쁘고(2.8~6.8배),
최적은 축을 카메라로 똑바로 향하는 것도 아닙니다. 그래서 규칙을 정하는 대신
**잰 값 자체를 최대화**합니다.

**옮겨서 나빠지면 옮기지 않습니다.** 파지 자세가 이미 그 관절을 잘 보는
경우가 있습니다 — 데스크 램프는 두 관절 축이 거의 나란하고 파지 자세에서
이미 4.4 px/deg 라, 전용 자세로 가면 오히려 4.2로 떨어졌습니다. 이득이
1.15배 미만이면 그냥 파지 자세에서 읽습니다 (`dual_view.MIN_VIEW_GAIN`).

- 새 파일: `my_work/study_startpose.py`, `my_work/study_startpose.png`
- 새 함수: `robot_scene.observability_px_per_deg`, `ViewScorer`,
  `find_viewing_poses`, `parallel_groups`
- 되돌리려면: `dual_view.py --no-view-poses`

### 카메라 캘리브레이션을 파이프라인이 씁니다 (실물 쪽 필수)

각도를 읽는 자세는 **카메라가 어디서 보느냐**로 정해집니다. 도면상의 명목
위치로 계산해 두고 실제 카메라가 10 cm 옆에 있으면, 고른 자세가 최적이
아니고 심하면 물체가 화면 밖으로 나갑니다. 카메라는 팔이 부딪힐 수 있는
**장애물**이기도 해서 충돌 판정도 함께 어긋납니다.

```bash
cd my_work
R=../robot_learning/scripts/run_drake_env.sh
$R python calibrate_camera.py --matrix <4x4, 숫자 16개> --rms-px 0.31
$R python calibrate_camera.py --show --check-poses     # 확인
```

`calibration/camera_cam_d456_front.json` 이 있으면 자동으로 이깁니다.
실행할 때마다 어느 값을 쓰는지 화면에 찍히고, 실물 모드인데 파일이 없으면
경고합니다. 자세한 것은 [calibration/README.md](calibration/README.md).

**캘리브레이션 값은 PC마다 다르므로 git에 올리지 않습니다** (`.gitignore`).
팀원끼리 파일을 주고받지 마세요.

- 새 파일: `my_work/calibrate_camera.py`, `calibration/README.md`

### 램프 스캔이 실제 색으로 보입니다

0813 `desk_lamp_minimal_sim` 배달물의 정점 색을 화면에 반영했습니다.
Drake는 메시 하나에 단색 하나만 입히므로(Meshcat으로 나가는 재질에
`vertexColors=false`가 박혀 나갑니다), 색이 비슷한 면끼리 6무리로 묶어
조각으로 나누고 무리마다 평균색을 줍니다.

색이 보랏빛인 것은 촬영 조명색이 스캔에 함께 구워졌기 때문입니다.
`export DESK_LAMP_WHITE_BALANCE=1` 로 보정할 수 있습니다.

배달물 경로는 세 곳을 찾습니다: `DESK_LAMP_DELIVERY` 환경변수 →
저장소 안 `assets/desk_lamp_minimal_sim` → `~/Downloads`.

### 고친 것

- **Robotiq 2F-85로 바꾸면서 남아 있던 PGC 전용 관절 이름** 때문에 sim 모드가
  시작도 못 하고 죽던 문제 (`finger1_joint` 하드코딩 → 그리퍼 정의에서 읽음)
- `requirements/drake.txt` 에 **scipy 누락** — 총최소제곱(TLS) 추정기가 쓰는데
  잠금 파일에 없어서 새 PC에서 설치하면 그 자리에서 깨졌습니다
- 로그로 넘겨 돌릴 때 마지막 URDF 질문에서 `EOFError` 로 죽던 문제

### 새로 생긴 실행 옵션

| 옵션 | 뜻 |
| --- | --- |
| `--no-view-poses` | 관절별 각도 측정 자세를 쓰지 않음 (예전 방식) |
| `--seed` | 측정 잡음·경로 계획의 난수 씨앗 |
| `DESK_LAMP_WHITE_BALANCE=1` | 스캔 색의 조명 치우침 보정 |
| `DESK_LAMP_DELIVERY=경로` | 램프 배달물이 저장소 밖에 있을 때 |
| `CAMERA_CALIBRATION=경로` | 캘리브레이션 파일을 다른 곳에 뒀을 때 |

---

## 2026-08-13 — 저장소 공개

- 두 PC(작업 PC / 로봇 PC)에서 `git clone` 만으로 환경을 세울 수 있도록 정리
- `setup/bootstrap.sh` 한 줄 설치, `setup/doctor.py` 12개 항목 자가 진단
- `SETUP.md`, `AGENTS.md`(AI용), `README.md`
- 폴더 이름 `HTD-main` → `PIVOT`
