# session_20260904_1736 — 왜 시뮬은 되고 실물은 실패했나 (정량 분석)

데이터: `experiments/session_20260904_1736/` (`experiment_results.json`,
`exploration_results.csv`, `grasp.json`, `preflight.json`, `posterior_round_0.json`).
원시 렌치는 저장되지 않아 오프라인 재추정은 불가능하다 (`raw_wrench_recovery.available: false`).

## 결론 — 한 문장

**회귀행렬은 파지점 기준으로 토크를 계산하는데 AFT200 은 센서 퍽 기준으로 재고,
그 사이 173.9 mm 를 옮겨주는 코드가 없었다.** 시뮬레이션은 예측과 진리가 같은
plant 의 같은 점에서 나오므로 이 오차가 구조적으로 존재할 수 없다.
수정(`GRASP_FRAME=measured`, `origin="aft"`)은 세션 **다음 날**(09‑05, `4a9e2ac`)
들어갔고, 지금은 실물 deploy 에서 강제된다.

## 1. 데이터가 보여주는 것

| 부위 | 추정 질량 | GT | 오차 |
|---|---:|---:|---:|
| link_3 (Arm) | 4.4 g | 85 g | **−94.8 %** |
| link_1 (Base) | 409.2 g | 399 g | +2.6 % |
| link_2 (Head) | 3.3 g | 87 g | **−96.2 %** |
| 합계 | 416.9 g | 571 g (저울) | **−27.0 %** |

| 라운드 | 밀도 [kg/m³] | 파지 오프셋 [mm] | 잔차팽창 | 반폭 |
|---|---|---|---:|---:|
| 1 | [**50**, 1104.7, **50**] | [**50.0, −50.0, 50.0**] | 6506.7 | 8,124,969 % |
| 2 | [**50**, 1420.1, **50**] | [37.35, **−50.0, 50.0**] | 5705.0 | 6,017,127 % |
| 3 | [**50**, 1500.3, **50**] | [37.14, **−50.0, 50.0**] | 5527.3 | 2,743,357 % |

굵은 값은 전부 한계(밀도 하한 50, 파지 오프셋 상자 ±50 mm)에 붙어 있다.

## 2. 결정적 증거 — 파지 오프셋이 상자에 붙은 **패턴**

실측 프레임 간격을 파지 오프셋으로 흡수하려면 `[+49.2, −82.0, +145.2] mm`
(크기 173.9 mm) 가 필요하다. 상자는 ±50 mm.

| 축 | 필요값 | 상자 안? | 관측 |
|---|---:|---|---|
| x | +49.2 | O | 50.0 → **37.1** (풀림) |
| y | −82.0 | X | **−50.0 고정** |
| z | +145.2 | X | **+50.0 고정** |

세 축의 **부호가 전부 일치**하고, 상자 안에 들어오는 x 만 풀린다. 다른 어떤
원인도 이 패턴을 만들지 못한다.

`tools/check_grasp_frames.py --grasp <세션 grasp.json>` 을 오늘 돌린 값:

```
회귀행렬 (obj_sensor) : [0, 0, 0] mm
AFT200 몸체 원점      : [-8.9, -40.9, 182.9] mm   회전 [132.1, -5.1, 24.7] deg
두 점 사이 거리       : 187.7 mm  ->  토크 오차 최대 1.051 N·m (센서 잡음의 350 배)
```

(173.9 와 187.7 의 차이는 파지점을 핀치 점으로 잡느냐 부피 도심으로 잡느냐 —
`desk_lamp.py` 의 105.8 mm — 가 다른 checkout 에서 계산된 탓으로 보인다.
어느 쪽이든 ±50 mm 밖이다.)

## 3. 보조 증거 — 잔차팽창의 크기

`residual_inflation = sqrt(Σ rᵀR⁻¹r / dof)` = 실제 잔차 ÷ 가정 잡음.
σ_T(1000 샘플, 바이어스 5 %) = 0.177 mN·m, 램프 무게 5.602 N.

| 라운드 | 팽창 | 잔차 토크 | 등가 모멘트팔 |
|---|---:|---:|---:|
| 1 | 6506.7 | 1.154 N·m | 206.1 mm |
| 2 | 5705.0 | 1.012 N·m | 180.7 mm |
| 3 | 5527.3 | 0.981 N·m | 175.1 mm |

파지 오프셋이 흡수해 감에 따라 206 → 181 → 175 mm 로 감소하며 실측 간격 쪽으로
간다. (자유도 정규화와 힘 채널 58.7 N 오프셋의 몫을 정확히 모르므로 0.7 %
일치는 우연일 수 있다 — 확실한 것은 **자릿수 10² mm** 다.)

## 4. 왜 시뮬레이션은 통과했나

`alg.measure()` 가 회귀행렬과 **같은** 물체 루트 기준으로 `tau = cross(p_com, f)`
를 만든다. 예측도 B, 정답도 B. 173.9 mm 지렛대는 실물 센서가 A 에서 잴 때만 생긴다.

## 5. 왜 가벼운 부위만 죽었나

같은 절대 오차가 가벼운 부위에서 큰 상대 오차가 된다. 무거운 link_1 이 오차를
흡수하며 커지고(1104 → 1500), 가벼운 두 부위는 하한 50 으로 밀린다.
램프 자산 도심오차 ablation 과 같은 구조다 (10 mm: link_1 12 %, link_3 75 %).

## 6. 원인이 **아닌** 것 — 여기에 시간 쓰지 말 것

- **각도**: 추천 vs FoundationPose 실측이 3라운드 모두 1° 이내.
  모형각↔물리각이 `116.43 − x`, `111.75 − x` 로 0.01° 이내 일정.
  시뮬 측정으로도 0.5°~3° 는 TLS 가 흡수 (질량 오차 0.2 % 이내).
- **정지 규칙**: `converged: false`, 반폭 2.7 M %, 경고 문구까지 정직하게 냈다.
- **preflight**: 11 항목 전부 OK. "각도 잔차 0.00 deg" 는 아핀 관계가 정확해서
  나온 값이지 의심 근거가 아니다.

## 7. 2차 원인 — 각각 독립적으로도 실패를 유발

| # | 문제 | 증거 | 수정 |
|---|---|---|---|
| A | `--prior water` 등방 사전분포 (총질량 안 묶임) | `prior [1000,1000,1000]`, 총질량 −27 % | `--prior weight` + `--total-mass-kg` (브랜치) |
| B | `GRASP_SIGMA_M = 5 mm` vs 173.9 mm = 35 σ | 상자 전에 사전분포가 막음 | 15~20 mm + measured 프레임 |
| C | 힘 채널 58.7 N 오프셋 (램프 5.60 N 의 10 배) | 타어 실패, 힘 3행이 회귀에 그대로 | 토크 전용 + `GravityTare` 0.5 N 게이트 |
| D | 원시 렌치 미저장 | 재분석 불가 | HEAD 에서 `exploration_round_N.json` 저장 |

## 8. 재발 방지 — 실험 전 확인

`TEAMMATE_CHECKLIST.md` §6‑0. 요약: cherry‑pick 브랜치 → `desk_lamp.py` 정의
확정 → 새 타어 0.5 N 통과 → conf(`GRASP_FRAME=measured`, `TOTAL_MASS_KG`,
`GRASP_SIGMA_MM=15`, `GRASP_MU_MM` 비움) → preflight 4 행 OK → 파지 후
`check_grasp_frames` 회전 수십 도면 중단 → 1라운드 후 `|F| ≈ 5.6 N` 검산.
