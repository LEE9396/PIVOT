# 실물 탐색 실패 원인 분석 — session_20260904_1736 (desklamp)

## 관측된 증상

| 증상 | 값 | 시뮬레이션 |
|---|---|---|
| 잔차팽창 | **5,527 ~ 6,507 배** | ~1 |
| Arm·Head 밀도 | **50 kg/m³** (하한에 붙음) | 정상 |
| Base 밀도 | 1,500 kg/m³ (GT 1,462 대비 −2.6%) | 정상 |
| 파지 오프셋 | **[50, −50, 50] mm** (세 축 전부 한계) | 0 |
| 보고 반폭 | **10⁶ %** 대 | < 5% |
| 추정 총질량 | **416.9 g** vs 저울 571.0 g (−27%) | 일치 |

---

## 먼저 — 기각된 가설 (여기에 시간 쓰지 말 것)

1. **"세 자세가 실제로는 안 돌았다"** — 아닙니다. 기록된 관절각으로 RB5 순기구학을 돌리면
   플랜지 기준 중력 방향이 **86.1° / 87.7° / 89.3°** 로 제대로 직교합니다.
   IK 제약(`robot_scene.py:813-817`, `AddAngleBetweenVectorsConstraint`, 허용오차 1.0°)이
   툴 방향을 세계 중력에 대해 올바르게 묶고 있고, 실제로 작동했습니다.
2. **"렌치가 중력을 따라 안 돈다"** — 아닙니다. 회전을 자유변수로 둔 강체 적합은
   중력항 **13.84 N** 을 줍니다. 관측된 방향 흔들림 19~27° 는
   `2·atan(13.84/58.7) = 26.5°` — **13.8 N 중력이 58.7 N 상수 오프셋 위에 얹힌 결과**로
   정확히 설명됩니다.
3. **`sensor_to_pivot` 전치 불일치** — 아닙니다. 이 값은 **한 번도 적용되지 않습니다.**
   `dual_view._load_tare` (`:2081-2083`) 는 `g_hat` 과 `wrench` 만 읽고
   `sensor_to_pivot`·`frame` 을 무시합니다. 전치 오류가 아니라 누락입니다.
4. **드라이버 자체 중력보상** — 그런 호출이 없습니다. `aft_tare.py:33-43` 은 홀딩 레지스터
   304–309 를 원시로 읽고 ×0.02 만 합니다. bias/zero 레지스터에 쓰지 않습니다.
   (다만 AFT200 컨트롤러가 코드 밖에서 내부 bias 를 들고 있을 가능성은 하드웨어로 확인해야 함)
5. **월드 좌표계에서 타어 차감** — 아닙니다. `hardware.TareTable.apply:109-115` 은
   센서 좌표계 상수 6-벡터를 그대로 뺍니다. 구조는 맞습니다.

---

## 원인 1 — 토크 기준점이 173.9 mm 어긋나 있다  ★확정, 지문 일치★

**회귀행렬은 파지점 기준 토크를 계산하는데, AFT200 은 센서 퍽 기준 토크를 잽니다.
그 사이를 옮겨주는 코드가 없습니다.**

- `density_id_drake.py:186` — `torque_rows = G_ACC * (np.cross(c, g_hat).T * VOLUMES)`,
  여기서 `c` 는 **물체 루트 프레임 기준 도심** (`:169-177`)
- `density_id_drake.py:123-125` — `plant.WeldFrames(world, bodies["base"], ...)`,
  주석에 *"base welded to sensor/world frame"* — **센서 원점 ≡ 파지점** 이라는 창립 가정
- `robot_scene.py:580-624` — 실제로는 AFT 마운트가 `tcp` 에서 +26.15 mm,
  그리퍼 베이스 +52.3 mm, 물체는 +147.7 mm 에 용접됨
- `dual_view.py:1886-1887` — `raw = read_raw(...)` → `self.tare.apply(g_hat, raw)`.
  **프레임 이동도, 모멘트팔 보정도 없음**

측정한 실제 간격:
```
AFT200 마운트 원점 (world) : [ 0.2074 -0.7400 1.9430]
모델 obj_sensor 원점 (world): [ 0.2074 -0.7400 2.1169]
간격 = 173.9 mm,  obj_sensor 좌표로 [-49.2, +82.0, -145.2] mm
```

### 왜 이것이 지문인가

이 간격을 파지 오프셋으로 흡수하려면 `delta = [+49.2, −82.0, +145.2] mm` 가 필요합니다.
그런데 상자 제약이 **±50 mm** (`design_core.py:343` `grasp_bound_m=0.05`) 이므로
최적화기는 **[+50, −50, +50]** 에 붙습니다.

**세션 라운드 1 의 실측값이 정확히 `[50.0, −50.0, 50.0]`** 입니다. 세 축의 부호까지 일치합니다.
라운드 2·3 에서 x 만 37 mm 로 풀리는 것도, 세 성분 중 x(49.2 mm)만 상자 안에 있기 때문입니다.

**고칠 곳**: `dual_view.read_one` 에서 `tau_model = tau_sensor − r × F` 로 렌치를 파지점으로
옮기거나, 모델의 토크 기준을 AFT200 몸체로 옮길 것. 그 뒤 `grasp_bound_m` 을 실제 장착
불확실성보다 크게 잡을 것.

**왜 시뮬은 통과했나**: `alg.measure` (`density_id_drake.py:195-205`) 가 회귀행렬과 **같은**
물체 루트 기준으로 `tau = cross(p_com, f)` 를 만듭니다. 시뮬은 구조적으로 자기모순이 없고,
173.9 mm 지렛대는 실물에서만 나타납니다.

---

## 원인 2 — 힘 부호 규약이 반대  ★유력, 3자세로는 확증 불가★

- `density_id_drake.py:185` — `force_rows = G_ACC * np.outer(g_hat, VOLUMES)` → **+ĝ 방향**
- `hardware_real.py:360-368` — 코드 자신의 주석: *"센서는 물체를 떠받치는 힘을 읽으므로
  f 는 −g_hat 쪽을 향해야 한다"* → **−ĝ 방향**

두 규약이 반대인데 실물 경로에서 조정되지 않습니다.
회전 자유 강체 적합은 **항등에서 175.8° 떨어진 R** 을 줍니다 — 즉 데이터가 `F ≈ −m·g·ĝ + b`
(반작용 규약)를 원합니다. 전역 부호 반전만으로도 잔차가 폭발합니다.

*단서: 3자세 / 미지수 7개 / 잔차 5.0 N 이라 R 이 느슨하게만 식별됩니다. 확증이 아니라 최우선 가설.*

**확인법**: `Aft200Sensor.check_sign_convention` 이 **이미 구현돼 있는데**
`hardware_real.self_check` (`:537`, `:541`) 에서만 불리고 **측정 경로에서는 한 번도 안 불립니다.**
물체를 쥔 직후 중력 방향마다 한 번씩 호출하면 3분에 끝납니다.

---

## 원인 3 — 타어가 상쇄 역할을 못 한다  ★확정★

| 항목 | 값 |
|---|---|
| 상수 오프셋 \|b\| | **58.7 N** |
| 중력항 m·g | **13.84 N** (1.411 kg) — 코드의 `TOOL_MASS_KG = 0.650` 의 **2.17배** |
| 강체모델 잔차 | **5.045 N** = 반복오차 0.034 N 의 **148배** |
| 램프 전체 무게 | **5.60 N** |

**상쇄되지 않는 자세의존 항이 신호 전체보다 큽니다.**

- `hardware.py:121-140` `run_tare` 주석: *"arm_poses 는 **탐색 때 쓸 자세와 같아야 한다**"*.
  강제되지 않고, `run_tare` 는 `self_check:600` 에서만 호출됩니다.
- 실제 경로는 `tare_real.py:313-395` 의 freedrive 수동 측정 파일을 씁니다.
  합격 기준이 **한 방향의 orientation_error 뿐** 이고, ĝ 축 둘레 회전과 팔 형상은 자유입니다.
- 측정 자세는 매 라운드 새로 풉니다 (`dual_view.py:1889-1911`).
  → **타어 자세 ≠ 측정 자세**
- `hardware_real.py:442-461` `TimedTare` 가 `record()` 시각을 찍는데
  `dual_view.py:2083` 이 **로드 시점에** 호출해서 30분 만료 검사(`TARE_MAX_AGE_S`)가 무력화됩니다.
  세션은 **69시간 된** 타어(09-01 22:19)를 09-04 17:36 에 재사용했습니다.

`EXPERIMENT_READY.md:246` 은 이 잔차를 *"자세별 표를 쓰는 한"* 무해하다고 적었지만,
그 전제가 깨져 있습니다.

---

## 원인 4 — 부호 없는 기하각을 부호 있는 관절각으로 그냥 먹인다  ★확정★

- `dual_view.py:2045-2049` — 코드가 스스로 적어둠:
  *"**부호와 영점은 아직 안 맞췄다**. 여기서 잇는 것은 '어느 키가 어느 관절인가' 뿐이다."*
- `dual_view.py:2137` — `angles = [float(data[k]) for k in keys]` 그대로
- → `:593` `measured = np.deg2rad(...)` → `:603` `A = alg.regressor(measured)`
- 생산자 쪽 `axis_geometry.py:56-57` 은 `arccos` 기반 **부호 없는 0~180° 기하각**

변환 도구 `tools/angle_signs.py` 가 있고 `calibration/angle_signs.json` 을 만듭니다.
그런데 그 파일을 읽는 곳은 **`tools/preflight.py:215-221` 한 곳뿐이며, 보고서 한 줄을 찍기 위해서입니다.
추정기는 절대 읽지 않습니다.** 측정하고, 표시하고, 버립니다.

더 나쁜 것: `joint_2_3` 의 q → 기하각 사상이 **단조가 아닙니다**
(q ∈ [−90°, +90°] 에서 3.8° → 157.5°, 접힘 1~2회).
세션에 보이는 `physical = 116.43 − model` 은 접힌 다대일 사상을 사후에 직선으로 근사한 것입니다.
기하각을 q 로 먹이면 head·support 도심이 최대 0.1~0.2 m 어긋나고, `A` 의 모든 행이 틀립니다.

*참고: `preflight.json` 의 "각도 부호·영점 잔차 최대 **0.00 deg**" 는 실제 40초 스윕에서
나올 수 없는 값입니다. 그 캘리브레이션은 실제로 수행되지 않았을 가능성이 큽니다.*

---

## 원인 5 — 저울 사전분포가 아예 안 걸렸다  ★확정★

- `pivot_ui.py:206` — **`"--prior", "water"` 하드코딩** (세션은 `pivot_ui` 로 실행됨)
- → `density_id_objects.py:300-306` `apply_water_prior`:
  `mu = 1000` 균일, `Sigma = diag(1000²)` **등방**, 그리고 `RHO_BOUNDS = (50.0, 20000.0)` (`:305`)
- `apply_weight_prior` (`:260-266`) 와 `weight_prior` (`:207-248`) — 총질량 방향을
  **0.2%** 로 묶는 그것 — 은 **한 번도 호출되지 않습니다**

즉 제가 앞서 본 `RHO_BOUNDS` 는 `apply_weight_prior:265` 가 아니라 `apply_water_prior:305` 의
같은 줄이었습니다. 그리고 railing 을 불가능하게 만들었을 **비등방 공분산이 `--prior water` 로 우회**됐습니다.

`dual_view.py:280` 이 실제 저울 총질량 0.571 kg 을 계산은 하지만, `grasp_columns` 스케일링에만
쓰이고 (`:618`, `:634`, `:646`) ρ 에 대한 구속으로는 **전혀 쓰이지 않습니다.**

결과 `[50, 1500.26, 50]` 의 총질량은 **416.9 g vs 저울 571.0 g — 27% 부족**.
`weight_prior` 였다면 0.2% 로 금지되는 해이고, 두 부위가 50 에 붙는 일도 없었습니다.

---

## 원인 6 — 어느 checkout 이 돌았는지 미확정  ★물리에 영향 있음★

`essential/1_pivot/desk_lamp_between_checkouts.diff` 의 `@@ -567,16 +571,6 @@` 에서
`PIVOT-real-setup` 은 `pinch["point"] = geometry[root]["centroid"]` 설정 블록을 **삭제**합니다.

로컬 자산에서 잰 차이: `link_3` 부피 도심 `[0.2601, 0.3930, 0.0688]` vs
볼록조각 pinch 점 `[0.2720, 0.3224, −0.0091]` → **105.8 mm**.
원인 1의 173.9 mm 위에 얹히고, long-axis 부호까지 뒤집힐 수 있습니다.

`MANIFEST.md` §1 은 `PIVOT-real-setup` 을 *"the checkout used by the failing launcher"* 라 하지만
(09-02 기준), 09-04 세션의 `preflight.json` 은 자산을 `/home/cheon/Desktop/PIVOT/assets/final_objects/lamp`
로 잡았고 "볼록 분해 3부위 모두 조각 있음", "빌드 일치" 를 보고했습니다.
**산출물만으로는 어느 `desk_lamp.py` 가 돌았는지 판정할 수 없습니다.**

**확인법**: 실제로 실행된 트리에서
`grep -c 'geometry\[root\]\["centroid"\]' <checkout>/my_work/desk_lamp.py`

---

## 작업 순서

1. **원인 1 부터.** 파지 오프셋 `[50, −50, 50]` 을 부호까지 재현하는 유일한 후보입니다.
   렌치를 파지점으로 옮기거나 모델 토크 기준을 AFT200 으로 옮기십시오.
2. **이미 있는데 안 불리는 안전장치 두 개를 켜십시오.**
   `Aft200Sensor.check_sign_convention` (원인 2, 3분)과
   실제 IK 자세에서의 `run_tare` (원인 3).
3. **`calibration/angle_signs.json` 을 추정기에 연결하십시오** (원인 4). 지금은 preflight 전용입니다.
4. **`--prior weight` 로 바꾸십시오** (원인 5). 그리고 **매 라운드 18개 원시 렌치를 저장하십시오** —
   세션 기록에 `raw_wrench_recovery.available: false` 라고 남아 있고, 이것 없이는
   어떤 것도 오프라인에서 재검증할 수 없습니다.
5. 어느 `desk_lamp.py` 가 돌았는지 확인 (원인 6).

원인 1, 4, 5 는 **하드웨어 없이 고칠 수 있고**, 원시 렌치만 저장돼 있었다면 원인 1·2·5 는
오프라인 재추정으로 즉시 검증됩니다.
